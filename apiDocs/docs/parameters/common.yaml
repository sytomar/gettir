parameters:
  # Query parameters
  Page:
    name: page
    description: Page Number
    in: query
    type: integer
    required: false
    default: 1
    minimum: 1
    maximum: 100
  Size:
    name: size
    description: Page size
    in: query
    type: integer
    required: false
    default: 10
    minimum: 1
    maximum: 1000