const recordsHelper = require('../controllers/records/helper');
const recordsModel = require('../mongo_models/records');
const ObjectId = require('mongoose').Types.ObjectId;

describe('Gettir tests', () => {
    test('adds 1 + 2 to equal 3', async () => {
        expect(3).toBe(3);
    });
});
